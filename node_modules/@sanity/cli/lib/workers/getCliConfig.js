"use strict";
var worker_threads = require("worker_threads"), getCliConfig = require("../_chunks-cjs/getCliConfig.js");
getCliConfig.getCliConfig(worker_threads.workerData, { forked: !1 }).then((config) => worker_threads.parentPort?.postMessage({ type: "config", config })).catch(
  (error) => worker_threads.parentPort?.postMessage({
    type: "error",
    error: error instanceof Error ? error.stack : error,
    errorType: error && (error.type || error.name)
  })
);
//# sourceMappingURL=getCliConfig.js.map
